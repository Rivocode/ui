// Sem esta referencia o `className` nao existe nas props de View, Text e
// Pressable, e o tsc reprova o catalogo inteiro por um erro que nao e dele.
// E o mesmo arquivo que o guia manda o app ter, aqui pelo mesmo motivo.
/// <reference types="nativewind/types" />
