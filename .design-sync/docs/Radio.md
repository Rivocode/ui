---
category: Formulario
---

# Radio

O circulo, sem rotulo. O texto fica num `<label>` que envolve os dois, igual ao
Checkbox, para o clique no texto tambem marcar.

Use quando as opcoes cabem na tela e comparar entre elas importa. Passando de
umas cinco, o `Select` gasta menos espaco.
