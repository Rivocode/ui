---
category: Formulario
---

# Input

Controle de texto. Vive dentro de `Field`.

`size`: `sm`, `md` (padrao) e `lg`, todos lendo a altura do token de densidade.
O atributo `size` nativo do HTML nao existe aqui de proposito, porque colidiria
com a variante.
